import type {AppConfig} from '~/config/environment/types';

// Mobile app configs for DEV
export const appConfig: AppConfig = {
  apiURL: 'https://jsonplaceholder.typicode.com/',
  apiKey: '',
  environment: 'develop',
};
